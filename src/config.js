const BASE_URL = ''

const ANNOUNCEMENTS = `${BASE_URL}/announcements/`;
const USERS = `${BASE_URL}/users/`;
const REQ_TOKEN = `${BASE_URL}${USERS}/req_token/`;


export {
    BASE_URL,
    USERS,
    ANNOUNCEMENTS,
    REQ_TOKEN,
};